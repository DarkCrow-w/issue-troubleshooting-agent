# 验证记录

验证日期：2026-09-16。

## 已通过

- Python 后端：53 项 pytest 测试；Ruff 检查通过。
- React：TypeScript 检查和 Vite 生产构建通过；Prettier 格式检查通过。
- 浏览器：3 项 Playwright 测试，分别覆盖完整排查/链路/证据/导出、移动端与清洗开关、取消交互；在本地开发入口和 Docker 入口均通过。
- 容器：三个镜像构建成功，React/Nginx → API Service → Agent Service 实际请求成功，端口仅绑定 `127.0.0.1:8080`。
- 当前容器接口复核：合成失败交易完成，2 个服务、3 个调用实例、2 条明确调用边；Markdown 含时间线，原始证据和配置快照可回查。
- 凭据扫描：交付代码中未发现写入的真实模型 API key。

## 本机 PostgreSQL 切换

- 实际连接 Homebrew PostgreSQL 17.10，主机 `127.0.0.1:5432`，独立数据库/账号 `tracelens`；Docker Agent 通过 `host.docker.internal:5432` 连接同一实例。
- 后端 48 项测试在真实 PostgreSQL 的独立临时 schema 中运行并通过，包含 JSONB 存取、过期任务与证据级联清理、重启恢复、迁移幂等性及失败事务回滚。
- Docker SQLite 备份迁入 12 个任务 / 95 条证据，本地 SQLite 备份迁入 7 个任务 / 56 条证据；19 个任务与 151 条证据逐条比对通过，原 ID、时间和 JSON 内容保留。
- 一致性备份位于 `data/backups/`，旧 SQLite 数据卷保留；这些备份需另行管理保留期限。
- PostgreSQL 部署后的 3 项浏览器测试通过。原先误建于 Docker 实例的空 TraceLens 数据库/角色已撤销，其他项目数据库未修改。
- 数据库凭据仅保存在本机被忽略的 `.env` 中（权限 0600），不写入文档或镜像。

## 模型实测（重构前基线）

使用用户授权的 qwen3.8-flash，通过文档指定的 OpenAI 兼容端点，发送的仅为合成数据。

- 连通性请求：HTTP 200，返回有效 JSON，用量 116 tokens。
- 完整诊断：完成 3 次日志检索（2 次补查）、2 次模型调用；模型返回输入 3,585 tokens、输出 5,470 tokens。
- 诊断识别了账户服务的 SQLTimeoutException、ACCT_TIMEOUT 响应和网关 502，并将慢 SQL/锁等待/网络等底层原因列为待验证假设。
- 重构前真实模型验证合计 9,171 tokens；未调用另外两个候选模型。
- 密钥仅用于临时请求，没有保存在项目文件中。启动默认仍为离线回放模式。

## LangChain / LangGraph 重构验证

- 本地 HTTP 服务器验证真实 LangChain + OpenAI SDK 请求路径、消息角色、结构化解析、usage 统计及 HTTP 500 不自动重试。
- LangGraph 实际补查循环覆盖 0 / 1 / 3 次预算；每次查询后重新执行规则提取。
- 模型调用预算耗尽后保留规则事实，任务输出部分结果。
- 模型调用中取消、分块之间取消、客户端关闭异常均保留已完成结果；供应商异常细节不返回前端。
- 模型 `finish_reason=length` 不接受为完整诊断，避免宽容 JSON 解析掩盖截断。
- 自定义模型 skill 的组合、证据展开与图节点执行记录通过回归测试。
- 真实 qwen3.8-flash 单条合成日志：有效 JSON 和证据引用通过，输入 789、输出 745 tokens。首次较开放的单条测试两次输出未通过校验，随后简洁提示词测试通过。后续完整交易调试确认固定 3,000 输出上限可全部被推理消耗：返回 content 为空、finish_reason=length、reasoning_tokens=3000。已将上限改为可配置的 8,192，并为输出预留上下文预算。失败均显式报告，未生成无效诊断。
- 调整输出预算后，真实 qwen3.8-flash 完整交易流程 `completed`：3 次查询（2 次补查）、2 次模型调用，输入 4,539 / 输出 6,205 tokens，总计 10,744 tokens；无告警，输出 8 条事实与 3 条假设，确认 SQL 超时 → ACCT_TIMEOUT → 网关 502 的传播，并明确排除另一交易上下文。此用量仅指最后一次成功的完整流程，不包含前面的调试调用。
- 本次未修改产品技能 manifest 或 HTTP 契约；未接入 LangSmith，未增加 checkpoint/自动续跑能力。

## 测试覆盖

关联 ID/复合 seqNo、嵌套 JSON、字段冲突、Java 异常链、原始值保留、相同正文和独立调用次数、业务码失败、并发/重试/时钟偏差、歧义调用 ID、无日志、清洗开关、超长材料分块、上下文交易隔离、SPL 转义和环境约束、分页与部分失败、字节/调用/时间预算、取消、重启恢复、过期清理、skill 依赖和自定义组合、完整证据展开、模型无效 JSON/无效引用与重试。

模型引用校验验证 ID 的存在，不等于证明每一条自然语言结论的语义正确。合成场景通过也不代表公司真实日志的准确率已经得到验证。

## 尚未验证

公司内网 Splunk 的真实连接、权限、字段适配及真实交易准确率，仍需要公司地址、index、凭据与带人工预期的完整样本。已实现的 Splunk 协议以 mock HTTP 验证；不将其表述为完成了真实内网联调。

另外两个候选模型支持通过配置切换，但没有实际调用验证。Python 测试输出有第三方 Starlette TestClient 弃用提示，不影响本轮测试结果。

## 可读性与诊断日志重构

- 报告页拆分为职责明确的视图，取消大段嵌套 return；保留原有极简页面和交互。
- 调用链构建改为提前跳过无效/歧义候选，原有显式/推测关联语义回归通过。
- 日志新增测试覆盖：错误正文不泄露、异步任务 ID 不串线、真实 LangGraph 查询日志关联、默认 INFO 不输出节点明细、清理连续故障不刷屏且可恢复。
- 52 项后端测试通过；前端生产构建、Prettier 与 Ruff 检查通过；Docker 入口 3 项浏览器回归测试通过。
- 实际容器日志核对：一次离线演示排查输出 9 条关键 JSON 事件，均关联同一任务 ID；不含交易关联 ID 或合成敏感字段。连续 3 次 health 请求没有产生访问日志。
